rules:
  - name: check_allowed_file_size
    module: rules.file
    class_name: FileSize
    description: "Check if size is allowed"
    type: "require"
    params:
      max_file_size: 10MB
    depends_on: []
